{
  "entities": {
    "User": {
      "title": "User",
      "description": "User profile and statistics",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "avatarUrl": { "type": "string" },
        "stats": { "type": "object" }
      },
      "required": ["id", "name"]
    },
    "Room": {
      "title": "Room",
      "description": "An online multiplayer room for Group Race",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "code": { "type": "string" },
        "hostId": { "type": "string" },
        "hostName": { "type": "string" },
        "status": { "type": "string" },
        "currentRound": { "type": "integer" },
        "wordLength": { "type": "integer" },
        "targetWord": { "type": "string" },
        "createdAt": { "type": "string" }
      },
      "required": ["id", "code", "hostId", "status"]
    },
    "RoomPlayer": {
      "title": "RoomPlayer",
      "description": "A player within a specific multiplayer room",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "avatar": { "type": "string" },
        "solved": { "type": "boolean" },
        "solvedRound": { "type": "integer" },
        "solveTime": { "type": "integer" },
        "currentAttempt": { "type": "integer" },
        "attemptsFeedback": { "type": "array" },
        "eliminated": { "type": "boolean" },
        "score": { "type": "integer" },
        "ready": { "type": "boolean" },
        "joinedAt": { "type": "string" }
      },
      "required": ["id", "name", "eliminated"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "User",
      "description": "User profiles"
    },
    "/rooms/{roomId}": {
      "schema": "Room",
      "description": "Online multiplayer rooms"
    },
    "/rooms/{roomId}/players/{playerId}": {
      "schema": "RoomPlayer",
      "description": "Players in an online multiplayer room"
    }
  }
}
